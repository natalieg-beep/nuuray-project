# ✅ UX-Fixes — 2026-02-08

## 📋 Zusammenfassung

Zwei wichtige UX-Probleme im Onboarding behoben:
1. Bottom Overflow im Name-Screen
2. TextField-Eingabe verschwindet bei Geburtsort-Autocomplete

---

## 🐛 Fix 1: Bottom Overflow im Name-Screen

**Problem:**
- `onboarding_name_screen.dart` hatte zu viel Content (4 Felder + Hinweis-Box)
- `Column` mit `Spacer()` ohne Scroll-Container
- Bei kleinen Bildschirmen: "Bottom Overflowed by 59 pixels"

**Lösung:**
```dart
// Vorher:
Padding(
  child: Form(
    child: Column(
      children: [
        // ...
        const Spacer(),
        ElevatedButton(...),
      ],
    ),
  ),
)

// Nachher:
SingleChildScrollView(
  child: Padding(
    child: Form(
      child: Column(
        children: [
          // ...
          const SizedBox(height: 32), // Fixed Spacing statt Spacer
          ElevatedButton(...),
          const SizedBox(height: 24), // Extra Padding am Ende
        ],
      ),
    ),
  ),
)
```

**Ergebnis:**
- ✅ Screen ist jetzt scrollbar
- ✅ Funktioniert auf allen Bildschirmgrößen
- ✅ Button klebt nicht am unteren Rand

**Datei:** `apps/glow/lib/src/features/onboarding/screens/onboarding_name_screen.dart`

---

## 🐛 Fix 2: Geburtsort-Autocomplete überschreibt Eingabe

**Problem:**
- User tippt "Münc" → Autocomplete findet "München, Deutschland"
- TextField wird **sofort überschrieben** mit gefundenem Ort
- User-Eingabe verschwindet → Frustrierendes UX

**Ursache:**
```dart
// In _performSearch() Zeile 165:
_searchController.text = _selectedPlace!; // ❌ Überschreibt User-Eingabe!
```

**Lösung:**
```dart
// Zeile 154-167:
setState(() {
  _selectedPlace = data['place'] as String;
  _selectedLatitude = (data['latitude'] as num).toDouble();
  _selectedLongitude = (data['longitude'] as num).toDouble();
  _selectedTimezone = data['timezone'] as String;
  _isSearching = false;
  _errorMessage = null;

  // ✅ NICHT den TextField überschreiben!
  // Der gefundene Ort wird in der grünen Success-Box angezeigt
});
```

**Verhalten jetzt:**
1. User tippt "Münc"
2. Suche findet "München, Deutschland"
3. TextField **bleibt** "Münc" ✅
4. Grüne Success-Box zeigt gefundenen Ort
5. User kann weitertippen oder Ort akzeptieren

**Ergebnis:**
- ✅ User-Eingabe bleibt erhalten während Suche
- ✅ Gefundener Ort wird nur in Success-Box angezeigt
- ✅ User hat volle Kontrolle über Eingabe

**Datei:** `apps/glow/lib/src/features/onboarding/screens/onboarding_birthdata_combined_screen.dart`

---

## 🎯 Status

**Beide Fixes deployed:**
- ✅ Name-Screen scrollbar
- ✅ Geburtsort-Autocomplete behält Eingabe

**Testing:**
- Hot Reload (`r`) notwendig
- Funktioniert auf iOS/Android/Web

---

## 🧪 Testing-Checklist

- [ ] Name-Screen auf kleinem Bildschirm (iPhone SE) testen
- [ ] Name-Screen scrollt flüssig
- [ ] Geburtsort-Autocomplete: Eingabe verschwindet nicht
- [ ] Geburtsort-Autocomplete: Gefundener Ort in grüner Box
- [ ] Geburtsort: Weitertippen nach erster Suche funktioniert

---

**Datum:** 2026-02-08
**Dauer:** ~20 Minuten
**Status:** ✅ Beide Fixes erfolgreich
